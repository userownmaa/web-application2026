<!-- {% extends 'base.html' %}

{% block content %}
<h1 class="my-4">Данные текущего запроса</h1>

<h3>Метод и URL</h3>
<p><strong>Метод:</strong> {{ data.method }}</p>
<p><strong>Полный URL:</strong> {{ data.url }}</p>

<h3>Параметры URL (args / query string)</h3>
{% if data.args %}
    <ul>
    {% for k, v in data.args.items() %}
        <li><strong>{{ k }}:</strong> {{ v }}</li>
    {% endfor %}
    </ul>
{% else %}
    <p class="text-muted">Нет GET-параметров</p>
{% endif %}

<h3>Данные формы (POST)</h3>
{% if data.form %}
    <ul>
    {% for k, v in data.form.items() %}
        <li><strong>{{ k }}:</strong> {{ v }}</li>
    {% endfor %}
    </ul>
{% else %}
    <p class="text-muted">Форма не отправлена (метод GET или пустая форма)</p>
{% endif %}

<h3>Заголовки запроса (headers)</h3>
<ul>
{% for k, v in data.headers.items() %}
    <li><strong>{{ k }}:</strong> {{ v }}</li>
{% endfor %}
</ul>

<h3>Cookie</h3>
{% if data.cookies %}
    <ul>
    {% for k, v in data.cookies.items() %}
        <li><strong>{{ k }}:</strong> {{ v }}</li>
    {% endfor %}
    </ul>
{% else %}
    <p class="text-muted">Нет cookie</p>
{% endif %}

<p class="mt-4">
    Попробуйте добавить параметры в URL: <code>/request?user=anna&role=student</code><br>
    Или отправьте форму ниже (это демонстрация POST).
</p>
{% endblock %} -->





<!-- <div class="mt-4">
        <a href="{{ url_for('index') }}" class="btn btn-outline-dark">На главную</a>
    </div> -->





{% extends 'base.html' %}

{% block content %}
<h1 class="my-4">Форма авторизации</h1>

{% if login_data %}
    <div class="alert alert-success">
        <h4>Данные успешно получены!</h4>
        <p><strong>Логин:</strong> {{ login_data.username }}</p>
        <p><strong>Пароль:</strong> {{ login_data.password }}</p>
    </div>
    <a href="{{ url_for('login') }}" class="btn btn-primary">Ввести другие данные</a>
{% else %}
    <form method="POST" class="w-50">
        <div class="mb-3">
            <label for="username" class="form-label">Логин / Email</label>
            <input type="text" class="form-control" id="username" name="username" required>
        </div>
        <div class="mb-3">
            <label for="password" class="form-label">Пароль</label>
            <input type="password" class="form-control" id="password" name="password" required>
        </div>
        <button type="submit" class="btn btn-primary">Войти</button>
    </form>
{% endif %}



<!-- <p class="mt-4 text-muted">
    После отправки формы данные отображаются на этой же странице.
</p> -->
{% endblock %}



<div class="card shadow-sm h-100">
                <div class="card-header bg-secondary text-white">
                    <h5 class="mb-0">Данные формы (POST)</h5>
                </div>
                <div class="card-body">
                    {% if data.form %}
                        <ul class="list-group list-group-flush">
                            {% for key, value in data.form.items() %}
                                <li class="list-group-item">
                                    <strong>{{ key }}:</strong> 
                                    {% if key == 'password' %}••••••••{% else %}{{ value }}{% endif %}
                                </li>
                            {% endfor %}
                        </ul>
                        <small class="text-muted mt-2 d-block">
                            Эти данные показываются только сразу после отправки формы
                        </small>
                    {% else %}
                        <p class="text-muted mb-0">Форма ещё не отправлялась или страница была обновлена</p>
                        <small class="text-muted">
                            Заполните и отправьте форму выше — данные появятся здесь
                        </small>
                    {% endif %}
                </div>
            </div>

